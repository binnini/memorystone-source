namespace SeoulPlayup.Map.Runtime
{
    /// <summary>
    /// 서비스 오브젝트를 출하에서 쓸지 말지의 <b>단일 스위치</b>(2026-08-31 사용자 확정: 공작소는 쓰지 않는다).
    ///
    /// <para>
    /// ⚠️ <b>2026-09-01 갱신: 맵 저작에서는 지웠다.</b> 원래는 「데이터는 지우지 않는다」는 방침이었으나,
    /// 서비스 추첨 배치 작업에서 사용자가 공작소 오브젝트 자체의 삭제를 확정해
    /// <c>Stage_1_Source</c>의 <c>workshop-127-n73</c> 엔트리가 사라졌다. 카탈로그·스펙·세이브
    /// 호환은 그대로 두었으므로, 되살리려면 <b>이 값을 뒤집는 것에 더해 맵에 엔트리를 다시
    /// 저작</b>해야 한다 — 스위치 하나로는 안 돌아온다.
    /// </para>
    ///
    /// <para>
    /// 스위치를 남겨 두는 이유: 시각·트리거 두 경로가 이 값을 보고 있고, 공작소가 다시 필요해질 때
    /// 그 배선을 새로 깔지 않아도 되게 하기 위해서다. 맵에 엔트리가 없는 지금은 아무 일도 하지 않는다.
    /// </para>
    ///
    /// <para>
    /// 🔴 끄는 자리는 <b>두 곳뿐</b>이다: ①시각(맵 오브젝트 스폰) ②트리거(밟았을 때). 둘 중 하나만
    /// 끄면 「보이는데 안 되는 물건」이나 「안 보이는데 밟히는 칸」이 된다.
    /// </para>
    /// </summary>
    public static class ServiceObjectAvailability
    {
        /// <summary>
        /// 공작소(카드 제거 ∥ 연마)를 맵에 세우고 밟을 수 있게 할지. 꺼져 있으면 시각도 트리거도 없다.
        /// 🔑 카드 제거·연마 자체는 사라지지 않는다 — 제거는 잡화점, 연마는 캠핑카가 계속 판다.
        /// </summary>
        public static readonly bool WorkshopEnabled = false;
    }
}
